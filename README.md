> 一个小工具，按下鼠标侧键打出 LOL 经典问号❓

### ✨ 功能

- 按下**鼠标侧键**，自动输入游戏里的问号 ping 信号
- 双击运行 `ping.exe` 会自动生成 `config` 配置文件
- 修改配置文件可以自定义：问号大小、触发按键
- 修改配置保存后，重启程序即可生效

### 📦 使用方法

1. 直接双击 `ping.exe` 运行程序
2. 首次运行会在同目录生成 `config` 配置文件
3. 使用鼠标侧键即可触发打出问号
4. 需要改参数：关闭程序 → 编辑 `config` 文件 → 保存，重新打开 `ping.exe`
